1：基础理论
对于 数据结构 可以用的 技巧方法都有什么  trik 是什么 怎样组织 数据和变量去做 完成对数据的筛选

data number code ---> function  include(trik)
2：二分查找：
	get：处理好边界，当步骤往下进行时，处理好边界，执行操作即可
	永远跟随总体目标进行设计
	其实主要就是对区间的定义没有理解清楚，在循环中没有始终坚持根据查找区间的定义来做边界处理。
	区间的定义就是不变量，那么在循环中坚持根据查找区间的定义来做边界处理，就是循环不变量规则。
3：移除元素（数组）
	快慢指针，移除数组（连续空间）中的元素
	python中指针可以用变量代替，直接使用 index[place] 
	根据最终目标安排 循环和分支走法
总结get way: MIKE 方法
关注数据
4：有序数组平方：
	 data：有负数 
	 存储：数组
	 目标：将数组平方 排序
	 条件: 请你设计时间复杂度为 `O(n)` 的算法解决本问题
	优化方法：从数据角度看 从数学角度 触发 设计 代码的流向 算法
		根据合适的DS 设计算法 完成 数据流转
5：长度最小的子数组
	data:  目标数 s 原数组[]
	goal：在[]找长度最小的连续数组
	条件:- 1 <= target <= 10^9
	- 1 <= nums.length <= 10^5
	- 1 <= nums[i] <= 10^5
	优化：
	方法：数组操作中：滑动窗口（ function tool) 滑动窗口 构建动态的窗口 进行筛选 组成:双指针，进行选定要操作的数组范围， 动态移动（本质思路）
